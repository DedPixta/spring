CREATE SCHEMA IF NOT EXISTS spring;

DROP TABLE IF EXISTS spring.secure_person;

CREATE TABLE spring.secure_person
(
    id         int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name       varchar(100) NOT NULL,
    pass       varchar(100) NOT NULL,
    birth_year int          NOT NULL CHECK ( birth_year BETWEEN 1800 AND 2023)
);

INSERT INTO
    spring.secure_person(name, pass, birth_year)
VALUES
    ('Jasmin', '123', 1985),
    ('Tom', '321', 1988),
    ('Jimmy', '111', 1997);